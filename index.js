// Ternary Operators
let points = 110;

let customerType = points > 100 ? "gold" : "silver";
// let variable = condition ? ifTrue : ifFalse;

// Operator Precedence
let x = 2 + 3 * 4; // returns as 14

let x = (2 + 3) * 4; // returns as 20

// operator precendence is similar in maths.
